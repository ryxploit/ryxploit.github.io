---
// Projects Section Component
---

<section id="proyectos" class="py-20 bg-gray-50">
  <div class="container mx-auto px-6 lg:px-8">
    <!-- Section Header -->
    <div class="text-center mb-16">
      <span class="inline-block px-4 py-2 bg-green-100 text-green-800 rounded-full text-sm font-medium mb-4">
        💼 Portafolio
      </span>
      <h2 class="text-3xl lg:text-4xl font-bold text-gray-800 mb-6">
        Proyectos <span class="text-gradient">Destacados</span>
      </h2>
      <p class="text-lg text-gray-600 max-w-2xl mx-auto">
        Una selección de proyectos que demuestran mi experiencia en desarrollo full stack, 
        diseño UI/UX y soluciones innovadoras
      </p>
    </div>
    


    <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 max-w-6xl mx-auto" id="projects-grid">
      <!-- Rentinno Project -->
      <div class="project-card card">
        <div class="p-8">
          <!-- Logo y título centrados -->
          <div class="text-center mb-6">
            <img src="https://rentinno.com.mx/img/logo_1_n.png" alt="Rentinno Logo" class="h-20 w-auto mx-auto mb-4">
            <h3 class="text-2xl font-bold text-gray-800">Rentinno</h3>
            <span class="inline-block bg-emerald-100 text-emerald-700 px-3 py-1 rounded-full text-sm font-medium mt-2">Co-fundador</span>
          </div>
          
          <!-- Descripción -->
          <p class="text-gray-600 mb-6 text-center leading-relaxed">Plataforma de gestión de propiedades en renta con calendario inteligente, notificaciones automáticas, recibos digitales y conexión directa con clientes.</p>
          
          <!-- Botones -->
          <div class="flex flex-col sm:flex-row gap-3 justify-center">
            <a href="https://rentinno.com.mx/app" target="_blank" class="btn-primary text-center px-6 py-3">
              <i class="fas fa-external-link-alt mr-2"></i>
              Ver Plataforma
            </a>
            <a href="https://rentinno.com.mx" target="_blank" class="btn-secondary text-center px-6 py-3">
              <i class="fas fa-globe mr-2"></i>
              Sitio Web
            </a>
          </div>
        </div>
      </div>

      <!-- Slary Project -->
      <div class="project-card card">
        <div class="p-8">
          <!-- Logo y título centrados -->
          <div class="text-center mb-6">
            <img src="/assets/slary.png" alt="Slary Logo" class="h-20 w-auto mx-auto mb-4">
            <h3 class="text-2xl font-bold text-gray-800">Slary</h3>
            <span class="inline-block bg-blue-100 text-blue-700 px-3 py-1 rounded-full text-sm font-medium mt-2">Fundador</span>
          </div>
          
          <!-- Descripción -->
          <p class="text-gray-600 mb-6 text-center leading-relaxed">Aplicación móvil diseñada para la gestión y organización de nóminas digitales. Permite importar, visualizar, organizar y compartir documentos de nómina de manera eficiente y segura.</p>
          
          <!-- Botones -->
                    <div class="flex flex-col sm:flex-row gap-3 justify-center">
                        <a href="/slary" class="btn-primary text-center px-6 py-3">
                            <i class="fas fa-info-circle mr-2"></i>
                            Ver Información
                        </a>
                        <a href="/slary#features" class="btn-secondary text-center px-6 py-3">
                            <i class="fas fa-star mr-2"></i>
                            Características
                        </a>
                    </div>
        </div>
      </div>
    </div>
    
    <!-- GitHub CTA -->
    <div class="text-center">
      <div class="bg-white rounded-2xl p-8 shadow-lg max-w-2xl mx-auto">
        <div class="w-16 h-16 bg-gray-100 rounded-full flex items-center justify-center mx-auto mb-6">
          <i class="fab fa-github text-gray-600 text-2xl"></i>
        </div>
        <h3 class="text-2xl font-bold text-gray-800 mb-4">
          ¿Quieres ver más proyectos?
        </h3>
        <p class="text-gray-600 mb-6">
          Explora mi repositorio completo en GitHub con más de 50 proyectos, 
          desde aplicaciones web hasta herramientas de automatización.
        </p>
        <a 
          href="https://github.com/ryxploit" 
          target="_blank" 
          rel="noopener noreferrer" 
          class="btn-primary inline-flex items-center"
        >
          <i class="fab fa-github mr-2"></i>
          Ver GitHub
        </a>
      </div>
    </div>
  </div>
</section>

<style>
  .project-card {
    transition: transform 0.3s ease, box-shadow 0.3s ease, opacity 0.5s ease;
    opacity: 1;
    transform: scale(1);
  }
  .project-card.hide {
    opacity: 0;
    transform: scale(0.9);
    pointer-events: none;
    height: 0;
    margin: 0;
    padding: 0;
    overflow: hidden;
  }
  .filter-btn {
    padding: 0.5rem 1.5rem;
    border-radius: 9999px;
    font-weight: 500;
    transition: all 0.3s ease;
    border: 2px solid transparent;
    background-color: #f8fafc;
    color: #64748b;
  }
  .filter-btn:hover {
    background-color: #e0e7ff;
    color: #3b82f6;
  }
  .filter-btn.active {
    background: linear-gradient(135deg, #3b82f6, #1d4ed8);
    color: white;
    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
  }
  
  .card {
    background: white;
    border-radius: 0.75rem;
    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
    transition: all 0.3s ease;
    overflow: hidden;
  }
  
  .card:hover {
    transform: translateY(-4px);
    box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
  }
  
  .btn-primary {
    background: linear-gradient(135deg, #3b82f6, #1d4ed8);
    color: white;
    padding: 0.5rem 1rem;
    border-radius: 0.5rem;
    text-decoration: none;
    font-weight: 500;
    transition: all 0.3s ease;
    display: inline-flex;
    align-items: center;
    justify-content: center;
  }
  
  .btn-primary:hover {
    background: linear-gradient(135deg, #1d4ed8, #1e40af);
    transform: translateY(-1px);
    box-shadow: 0 4px 12px rgba(59, 130, 246, 0.4);
  }
  
  .btn-secondary {
    background: #f3f4f6;
    color: #6b7280;
    padding: 0.5rem;
    border-radius: 0.5rem;
    text-decoration: none;
    transition: all 0.3s ease;
    display: inline-flex;
    align-items: center;
    justify-content: center;
  }
  
  .btn-secondary:hover {
    background: #e5e7eb;
    color: #374151;
    transform: translateY(-1px);
  }
  
  .tech-tag {
    padding: 0.25rem 0.75rem;
    border-radius: 9999px;
    font-size: 0.75rem;
    font-weight: 500;
    transition: all 0.2s ease;
  }
  
  .tech-tag:hover {
    transform: scale(1.05);
  }
  
  .text-gradient {
    background: linear-gradient(135deg, #3b82f6, #8b5cf6);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const filterButtons = document.querySelectorAll('.filter-btn');
    const projectCards = document.querySelectorAll('.project-card');

    filterButtons.forEach(button => {
      button.addEventListener('click', () => {
        // Update active button
        filterButtons.forEach(btn => btn.classList.remove('active'));
        button.classList.add('active');

        const filter = button.getAttribute('data-filter');

        projectCards.forEach(cardEl => {
          const card = cardEl as HTMLElement;
          if (filter === 'all' || card.getAttribute('data-category') === filter) {
            card.classList.remove('hide');
          } else {
            card.classList.add('hide');
          }
        });
      });
    });
  });
</script>