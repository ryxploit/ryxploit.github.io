---
// About Section Component
---

<section id="sobre-mi" class="py-20 bg-bg-secondary relative overflow-hidden">
  <div class="container mx-auto px-6 lg:px-8">
    <div class="absolute top-0 left-0 w-64 h-64 bg-blue-500/5 rounded-full -translate-x-1/2 -translate-y-1/2"></div>
    <div class="absolute bottom-0 right-0 w-64 h-64 bg-purple-500/5 rounded-full translate-x-1/2 translate-y-1/2"></div>
    <div class="grid lg:grid-cols-2 gap-16 items-center relative z-10">
      <!-- Content Column -->
      <div>
        <div class="mb-8">
          <span class="inline-block px-4 py-2 bg-blue-100 text-blue-800 rounded-full text-sm font-medium mb-4">
            💼 Sobre Mí
          </span>
          <h2 class="text-3xl lg:text-4xl font-bold text-gray-800 mb-6">
            Transformando <span class="text-gradient">Ideas</span> en 
            <span class="text-gradient">Realidad Digital</span>
          </h2>
        </div>
        
        <div class="space-y-6 text-gray-600 leading-relaxed">
          <p class="text-lg">
            Soy un <strong>desarrollador full stack apasionado</strong> con más de 3 años de experiencia 
            creando soluciones digitales innovadoras. Mi enfoque se centra en combinar 
            <strong>código limpio</strong>, <strong>diseño excepcional</strong> y <strong>experiencia de usuario</strong> 
            para entregar productos que realmente marquen la diferencia.
          </p>
          
          <p>
            Mi experiencia abarca desde el desarrollo de aplicaciones web modernas con 
            <strong>React y Vue.js</strong>, hasta aplicaciones móviles nativas con <strong>Flutter</strong>, 
            pasando por el diseño de interfaces intuitivas y sistemas backend robustos.
          </p>
          
          <p>
            Me especializo en crear <strong>experiencias digitales completas</strong>, desde la 
            conceptualización y diseño UI/UX hasta la implementación técnica y despliegue, 
            siempre con un enfoque en la <strong>calidad</strong> y <strong>escalabilidad</strong>.
          </p>
        </div>
        
        <!-- Specialties -->
        <div class="mt-8">
          <h3 class="text-xl font-semibold text-gray-800 mb-4">Mis Especialidades:</h3>
          <div class="grid grid-cols-2 gap-y-3 gap-x-6">
            <div class="flex items-center space-x-3">
              <div class="w-2 h-2 bg-blue-500 rounded-full"></div>
              <span class="text-gray-600">Desarrollo Web Moderno</span>
            </div>
            <div class="flex items-center space-x-3">
              <div class="w-2 h-2 bg-purple-500 rounded-full"></div>
              <span class="text-gray-600">Aplicaciones Móviles</span>
            </div>
            <div class="flex items-center space-x-3">
              <div class="w-2 h-2 bg-cyan-500 rounded-full"></div>
              <span class="text-gray-600">Diseño UI/UX</span>
            </div>
            <div class="flex items-center space-x-3">
              <div class="w-2 h-2 bg-green-500 rounded-full"></div>
              <span class="text-gray-600">APIs & Backend</span>
            </div>
            <div class="flex items-center space-x-3">
              <div class="w-2 h-2 bg-orange-500 rounded-full"></div>
              <span class="text-gray-600">Automatización</span>
            </div>
            <div class="flex items-center space-x-3">
              <div class="w-2 h-2 bg-pink-500 rounded-full"></div>
              <span class="text-gray-600">DevOps & Deploy</span>
            </div>
          </div>
        </div>
        
        <!-- CTA Buttons -->
        <div class="flex flex-col sm:flex-row gap-4 mt-8">
          <a href="/cv-carlos-leon.pdf" download class="btn-primary">
            <i class="fas fa-download"></i>
            Descargar CV
          </a>
          <a href="#contacto" class="btn-secondary">
            <i class="fas fa-comments"></i>
            Hablemos
          </a>
        </div>
      </div>
      
      <!-- Image & Stats Column -->
      <div class="space-y-8">
        <!-- Profile Image -->
        <div class="relative">
          <div class="relative w-full max-w-md mx-auto">
            <div class="aspect-square rounded-2xl overflow-hidden shadow-2xl">
              <img 
                src="/perfil.jpg" 
                alt="Carlos León trabajando" 
                class="w-full h-full object-cover"
              >
            </div>
            <!-- Decorative Elements -->
            <div class="absolute -top-4 -right-4 w-24 h-24 bg-blue-500 rounded-full opacity-20 animate-pulse-slow"></div>
            <div class="absolute -bottom-4 -left-4 w-16 h-16 bg-purple-500 rounded-full opacity-30 animate-pulse-slow" style="animation-delay: 1s;"></div>
          </div>
        </div>
        
        <!-- Stats Cards -->
        <div class="grid grid-cols-2 gap-4">
          <div class="card text-center p-6 bg-white/50 backdrop-blur-sm">
            <div class="text-3xl font-bold text-blue-600 mb-2 counter" data-target="3">0</div>
            <div class="text-sm text-gray-600">Años de<br>Experiencia</div>
          </div>
          <div class="card text-center p-6 bg-white/50 backdrop-blur-sm">
            <div class="text-3xl font-bold text-purple-600 mb-2 counter" data-target="50">0</div>
            <div class="text-sm text-gray-600">Proyectos<br>Completados</div>
          </div>
          <div class="card text-center p-6 bg-white/50 backdrop-blur-sm">
            <div class="text-3xl font-bold text-cyan-600 mb-2 counter" data-target="15">0</div>
            <div class="text-sm text-gray-600">Tecnologías<br>Dominadas</div>
          </div>
          <div class="card text-center p-6 bg-white/50 backdrop-blur-sm">
            <div class="text-3xl font-bold text-green-600 mb-2 counter" data-target="100">0</div>
            <div class="text-sm text-gray-600">Satisfacción<br>del Cliente</div>
          </div>
        </div>
        
        <!-- Achievement Badges -->
        <div class="bg-white/50 backdrop-blur-sm rounded-xl p-6 shadow-lg">
          <h4 class="font-semibold text-gray-800 mb-4">Logros Destacados</h4>
          <div class="space-y-3">
            <div class="flex items-center space-x-3">
              <div class="w-8 h-8 bg-blue-100 rounded-full flex items-center justify-center">
                <i class="fas fa-trophy text-blue-600 text-sm"></i>
              </div>
              <span class="text-sm text-gray-600">Apps publicadas en Play Store & App Store</span>
            </div>
            <div class="flex items-center space-x-3">
              <div class="w-8 h-8 bg-green-100 rounded-full flex items-center justify-center">
                <i class="fas fa-rocket text-green-600 text-sm"></i>
              </div>
              <span class="text-sm text-gray-600">Proyectos con +10K usuarios activos</span>
            </div>
            <div class="flex items-center space-x-3">
              <div class="w-8 h-8 bg-purple-100 rounded-full flex items-center justify-center">
                <i class="fas fa-star text-purple-600 text-sm"></i>
              </div>
              <span class="text-sm text-gray-600">Certificaciones en tecnologías modernas</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const counters = document.querySelectorAll('.counter');
    const speed = 200; // The lower the slower

    const animateCounters = () => {
      counters.forEach(counterEl => {
        const counter = counterEl as HTMLElement;
        const updateCount = () => {
          const targetAttr = counter.getAttribute('data-target');
          if (targetAttr === null) {
            return;
          }
          const target = +targetAttr;
          const count = +counter.innerText;

          const inc = target / speed;

          if (count < target) {
            counter.innerText = `${Math.ceil(count + inc)}`;
            setTimeout(updateCount, 1);
          } else {
            counter.innerText = `${target}`;
          }
        };

        updateCount();
      });
    };

    const observer = new IntersectionObserver((entries, observer) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          animateCounters();
          observer.unobserve(entry.target);
        }
      });
    }, { threshold: 0.5 });

    const aboutSection = document.querySelector('#sobre-mi');
    if (aboutSection) {
      observer.observe(aboutSection);
    }
  });
</script>