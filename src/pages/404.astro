---
import Layout from '../layouts/Layout.astro';
---

<Layout title="404 - Página no encontrada | ryxploit">
	<main class="min-h-screen flex items-center justify-center bg-gradient-to-br from-gray-900 via-blue-900 to-purple-900">
		<div class="text-center px-6">
			<div class="mb-8">
				<h1 class="text-9xl font-bold text-transparent bg-clip-text bg-gradient-to-r from-blue-400 to-purple-600 mb-4">
					404
				</h1>
				<h2 class="text-3xl font-bold text-white mb-4">
					Página no encontrada
				</h2>
				<p class="text-xl text-gray-300 mb-8 max-w-md mx-auto">
					Lo siento, la página que buscas no existe o ha sido movida.
				</p>
			</div>
			
			<div class="space-y-4">
				<a 
					href="/" 
					class="inline-block px-8 py-3 bg-gradient-to-r from-blue-500 to-purple-600 text-white font-semibold rounded-lg hover:from-blue-600 hover:to-purple-700 transition-all duration-300 transform hover:scale-105"
				>
					🏠 Volver al inicio
				</a>
			</div>
			
			<div class="mt-12 text-gray-400">
				<p class="text-sm">
					¿Necesitas ayuda? 
					<a href="/#contacto" class="text-blue-400 hover:text-blue-300 underline">
						Contáctame
					</a>
				</p>
			</div>
		</div>
	</main>
</Layout>

<style>
	@keyframes float {
		0%, 100% { transform: translateY(0px); }
		50% { transform: translateY(-10px); }
	}
	
	h1 {
		animation: float 3s ease-in-out infinite;
	}
</style>